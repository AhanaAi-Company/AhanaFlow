# Branch 33 Event Streams - Core Server Dependencies
# UniversalStateServer + VectorStateServerV2

# VectorStateServerV2 needs numpy for vector operations
numpy>=1.24.0

# ─── License Webhook Server (stripe_webhook/server.py) ───────────────
# Stripe payment events → JWT license issuance → email delivery
fastapi>=0.115.0
uvicorn[standard]>=0.29.0
stripe>=7.0.0
cryptography>=42.0.0
